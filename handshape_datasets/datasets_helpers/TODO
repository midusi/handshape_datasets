Nus_1 dataset:
    For BW:
        ☐ Create the x array with np.zero (as uint8). After Load the images
        Deal with the image classes:
            ☐ Parse the images names (g3(image)). Number after the g is the image class
            ☐ Create the y label with the classes of the image
        ☐ Create the metadata optional array.
    For Color:
        ☐ Create the x array with np.zero. After Load the images
        Deal with the image classes:
            ☐ Parse the images names (g3(image)). Number after the g is the image class
            ☐ Create the y label with the classes of the image
        ☐ Create the metadata optional array.

DatasetLoader Class:
    ☐ Method clear with docstring("dataset",path="/home/.handshape_datasets") @started(19-03-16 11:43)
    ☐ Method clear_all(path="/home/.handshape_datasets)

Document all the functions:
    For all datasets_helpers:
        ☐ Check if the dataset inherits the docstrings from Dataset_Loader
    DatasetLoader:
        ☐ download_dataset
        ☐ get
        ☐ get_download_flag
        ☐ get_preprocessed_flag
        ☐ get_status_flag
        ☐ load
        ☐ preprocess
        ☐ set_downloaded
        ☐ set_preprocessed_flag
        ☐ set_status_flag
    Dataset:
        Summary:
            ☐ Add pandas stats
            ☐ Make the docstring
        ☐ Show_images
    Init.py:
        ☐ Load
        ☐ Clear
        ☐ Clear all
    Utils:
        ☐ download_file_content_as_text
        ☐ download_file
        ☐ download_from_drive
        ☐ download_file_over_ftp
        ☐ download_bigger_file
        ☐ extract_zip
        ☐ extract_tar
        ☐ mkdir_unless_exists
        ☐ show_images
        ☐ show_subset
        ☐ show_dataset


Refactory to class:
    ☐ Irish
    ☐ Lsa16
    ☐ Pugeault
    To dataset:
        ☐ class Dataset(object):
            def __init__(self, name, images,metadata):
                self.name = name
                self.x=images
                self.y=klass
                self.metadata=metadata
        